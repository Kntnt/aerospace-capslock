# AeroSpace Harmony includes this file automatically when Ghostty is installed.
# Use separate terminal windows to avoid native macOS tab tiling conflicts.
# Cmd+N already opens a window; make Cmd+T do the same.
keybind = super+t=new_window
